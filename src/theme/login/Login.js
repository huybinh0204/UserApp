import React, {Component} from 'react';
import {Text, View,TouchableOpacity,Image} from 'react-native';
import LoginStackp from "./LoginStack";

class Login extends Component {
    render() {
        return (
            <LoginStackp/>
        );
    }
}
export default Login;
